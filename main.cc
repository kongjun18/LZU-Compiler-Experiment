#include "parser.h"

#include <string>
using namespace std::literals;

int main() {
	auto test_case = "main()\n{\nint i = 10;\nwhile (i) i = i - 1;\n}"s;
	// auto pairs =  GetPair(test_case);
	// PrintPairs(pairs);
	return 0;
}
